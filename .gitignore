# OS / editor junk
.DS_Store
**/.DS_Store
Thumbs.db
_test_ops/        # stray sandbox test dir — not part of the site
_arhive/

# docs
CLAUDE.md

# Slim the repo — intermediate render artifacts the live site does NOT link to.
# Already-tracked copies need a one-time `git rm -r --cached` (see commit recipe).
**/*_slides_en/            # intermediate PNG slides (the linked *_deep-dive_en.pdf are kept)
**/gof_*.pdf               # old GoF posters, replaced by *_deep-dive_en.pdf

# Optional — uncomment to slim further:
# linkedin/frames/         # MP4 source frames (the .mp4 is kept)
# _src/node_modules/       # if you ever install deps inside _src
